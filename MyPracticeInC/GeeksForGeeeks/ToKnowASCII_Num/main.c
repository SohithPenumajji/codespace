#include <stdio.h>
#include <stdlib.h>

int main()
{
    char c;
    printf("Enter that one Keyword to check its ASCII Value\n");
    scanf("%c", &c);
    printf("The keyword %c ASCII value is %d\n", c, c);
    return 0;
}
